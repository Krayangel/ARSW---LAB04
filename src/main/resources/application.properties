spring.mvc.pathmatch.matching-strategy=ant_path_matcher

# se actualiza el application properties para realizar la conexion a la base de datos

# Configuración de PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/blueprintsdb
spring.datasource.username=postgres
spring.datasource.password=12345
spring.datasource.driver-class-name=org.postgresql.Driver

# Ejecutar scripts SQL al iniciar
#spring.sql.init.mode=always
#spring.sql.init.schema-locations=classpath:schema.sql
#spring.sql.init.data-locations=classpath:data.sql

# Activar perfil postgres
spring.profiles.active=postgres


debug=true